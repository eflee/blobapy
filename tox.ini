[tox]
envlist = py34, py35, integ

[testenv]
deps = pytest
       flake8
       coverage
commands =
    coverage run --branch --source=blobapy -m py.test --ignore=tests/integ
    coverage report -m
    flake8 blobapy tests


[testenv:integ]
deps = pytest
commands =
    py.test --ignore=tests/unit
